const user = {
    name : 'Sangeeta',
    age : 25,
    occupation : 'Student',
}

for(const keys in user) {
    console.log(keys);
}

const arr = ['Sangeeta',25,'Student']

for(const index in arr) {
    console.log(index);
}