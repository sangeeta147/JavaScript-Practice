let arr = [1,2,3,4,5];

console.log(arr.join());  //by default (,) is used for separation

/* console.log(arr.join(' ')); - will be separated by gap
console.log(arr.join('-')); - will be separated by (-)*/

