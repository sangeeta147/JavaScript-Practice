var name = "Sangeeta";

var age = 25; /*Variables declared with var are function-scoped or globally-scoped and can be re-declared and updated.*/

console.log(name, age);

{
    var gender = "Female"; /*Variables declared with var are function-scoped or globally-scoped and can be re-declared and updated.*/
}

console.log(gender);

var city;

city = "Delhi"

console.log(city);