/*BOM (Browser Object Model) allows JavaScript to interact with the browser.
The window object is the global root of BOM.
The DOM is part of BOM, but BOM extends beyond the document.
BOM provides access to:
    History (navigating pages)
    Location (URL handling)
    Storage (Session & Local Storage)
    Timers (setTimeout, setInterval)
    Fetch API (network requests)
    Console & Alerts (debugging & user interaction)
*/


//HISTORY - navigation between pages
//window.history.back();
//window.history.forward();
//window.location.reload();

//TIMERS - Delays and Intervals
//setTimeout(() => console.log("Hello after 2 seconds"),2000);
//setInterval(() => cosole.log("Repeating every 1 second"),1000);

/*ALERTS & CONSOLE Logging
alert("This is an alert!");
console.log("Hello from BOM");
*/